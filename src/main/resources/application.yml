spring:
  application:
    name: product-service

  cloud:
    config:
      uri: http://localhost:8888

server:
  port: 9001  # Set a custom port

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
